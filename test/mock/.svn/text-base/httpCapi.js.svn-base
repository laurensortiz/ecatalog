angular.module('select.mocks')
    .service('mockHttpCapi', function () {
      return {
        generateBaseApiUrl: function () {
          return "";
        },
        generateCommonAPIHeaders: function () {
          return {};
        },
        generateFilesUrl: function (url) {
          return url;
        },
        getFileResource: function (url) {
          //TODO: Change httpCapi to not use promise. It isn't really doing anything async.
          return url;
        },
        translateToLocalFileUrl: function (url) {
          return url;
        }
      }
    });