'use strict';

describe('Service: SyncConfig', function () {
  var SyncConfig;
  beforeEach(module('conductivEcatalogApp'));
  beforeEach(inject(function (_SyncConfig_) {
    SyncConfig = _SyncConfig_;
  }));

  it('should do something', function () {
    expect(!!SyncConfig).toBe(true);
  });
});