/*'use strict';

describe('Controller: LoginCtrl', function () {

  // load the controller's module
  beforeEach(module('conductivEcatalogApp'));

  var LoginCtrl, scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller) {
    scope = {};
    LoginCtrl = $controller('LoginCtrl', {
      $scope: scope
    });
  }));*/

//
//  it('should initialize blank attributes', function () {
////    expect(scope.loginInfo.username).toBe('');
////    expect(scope.loginInfo.password).toBe('');
////    expect(scope.loginInfo.organization).toBe('');
////    expect(scope.loginInfo.rememberUsernameOrg).toBe(false);
////    expect(scope.loginInfo.rememberPass).toBe(false);
//  });
//});
