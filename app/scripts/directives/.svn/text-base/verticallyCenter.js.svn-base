'use strict';

angular.module('conductivEcatalogApp')
    .directive('verticallyCenter', function () {
        return {
            restrict: 'A',
            link: function postLink(scope, element, attrs) {
                $(element).flexVerticalCenter();
            }
        };
    });
